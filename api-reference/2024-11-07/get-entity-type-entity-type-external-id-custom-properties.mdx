---
title: 'Retrieve all Custom Properties of an entity'
openapi: 'GET /{entity_type}/{entity_type_external_id}/custom_properties'
---

Fetch all entity's Custom Properties from the system (if any exists, otherwise an empty list is returned).
